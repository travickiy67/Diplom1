#cloud-config
 users:
  - name: travitskii
    groups: sudo
    shell: /bin/bash
    sudo: ['ALL=(ALL) NOPASSWD:ALL']
    ssh-authorized-keys:
#    Строка ssh-authorized-keys: пустая, ssh_key через переменную с параметром  sensitive = true
